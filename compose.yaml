services:
  postgres:
    image: 'postgres:16.9-alpine'
    environment:
      - 'POSTGRES_DB=identityforge'
      - 'POSTGRES_PASSWORD=admin'
      - 'POSTGRES_USER=admin'
    ports:
      - '5432:5432'
